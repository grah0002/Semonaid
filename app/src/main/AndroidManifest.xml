<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gerontechies.semonaid">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MentalWellbeing.Events.EventInfoActivity"></activity>
        <activity android:name=".Activities.MentalWellbeing.Events.EventsCategoryActivity" />
        <activity android:name=".Activities.MentalWellbeing.MentalMenuActivity" />
        <activity android:name=".Activities.MentalWellbeing.Events.EventsListingActivity" />
        <activity android:name=".Activities.Income.ApplyJobs.JobsInstructionActivity" />
        <activity android:name=".Activities.Income.Skills.SkillsFilterActivity" />
        <activity
            android:name=".Activities.Income.Skills.SkillDetailItemActivity"
            android:theme="@style/Material" />
        <activity android:name=".Activities.Income.IncomeMenuActivity" />
        <activity android:name=".Activities.Income.Skills.SkillListingActivity" />
        <activity android:name=".Activities.Income.Skills.SkillsQuizActivity" />
        <activity android:name=".Activities.Budget.Calculator.TipsActivity" />
        <activity android:name=".Activities.GetStartedActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.Services.ServicesMapActivity"
            android:label="@string/title_activity_services_map" />
        <activity android:name=".Activities.Services.ServicesFilterActivity" />
        <activity android:name=".Activities.PasswordActivity">

            <!--
                <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".Activities.Budget.Tips.TipCategoryActivity" />
        <activity android:name=".Activities.Budget.Tips.MenuActivity" />
        <activity android:name=".Activities.Services.ServiceItemActivity" />
        <activity android:name=".Activities.Services.ServiceCategoryActivity" />
        <activity android:name=".Activities.Services.ServicesCategoryList" />
        <activity android:name=".Activities.Services.ServicesAllActivity" />
        <activity android:name=".Activities.Services.ServiceInfoActivity" />
        <activity android:name=".Activities.Budget.Calculator.SummaryActivity" />
        <activity android:name=".Activities.Budget.Calculator.TransportActivity" />
        <activity android:name=".Activities.Budget.Calculator.PersonalActivity" />
        <activity android:name=".Activities.Budget.Calculator.BillsActivity" />
        <activity android:name=".Activities.Budget.Calculator.HousingExpensesActivity" />
        <activity android:name=".Activities.Budget.Calculator.IncomeActivity" />
        <activity android:name=".Activities.Budget.BudgetMainMenuActivity" />
        <activity android:name=".Activities.Budget.BudgetInfoActivity" />
        <activity android:name=".Activities.HomeScreenActivity" />
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>